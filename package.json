{
  "name": "henohenon-github-io",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && bun run scripts/build-og.ts",
    "preview": "astro preview",
    "check": "astro check",
    "format": "biome format --write .",
    "lint": "biome check .",
    "generate-theme": "bun run scripts/generate-theme.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.98.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "@biomejs/biome": "^1.9.0",
    "@resvg/resvg-js": "^2.6.2",
    "@types/bun": "^1.1.0",
    "typescript": "^5.6.0",
    "zod": "^3.23.0"
  }
}
