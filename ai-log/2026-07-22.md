# 2026-07-22 作業ログ

## やったこと
- `docs/`（frame / introduction / GlobeXplore / MwP / kotohakobi）と現状コード（`src/main.ts` は skeleton のみ）を読み込み、設計メモを**確定仕様＋実装フェーズ計画**に整理。
- `ai-log/spec-and-plan.md` を新規作成。
- 主要な技術方針を対話で確定し、同ファイルへ反映。

## 決定事項
1. **展示枚数**: 当面 **3 件**（GlobeXplore / Make with Puppet / コトハコビ）。`main.ts` のプレースホルダ 6→3。各展示の **Icon・Main は当面「仮 or 空」**で、先に枠と遷移を通す。
2. **フレームワーク**: 導入しない。**バニラ TS + ハッシュルーター + View Transitions API**。
   - 理由: 核心の「作品別トランジション」は結局どの FW でも自前アニメになる。素の DOM を全握りする方が演出を作りやすく、規模的に FW の定型コストが割に合わない。次点候補は Svelte（今回不要）。
3. **ルーティング/遷移モデル**: **URL 分離（ハッシュ `#/focus/:id`）**。GitHub Pages でサーバ設定不要なため。将来クリーンパスにするなら `index.html`→`404.html` の SPA フォールバックで移行可。
4. **LLM 会話**: 別スコープ。v1 は**ランダム単語応答**（吹き出し UI ＋ 応答語配列からランダム選択）まで。LLM 連携は Phase 6。
5. **デプロイ**: **GitHub Pages**。
6. **命名整合**: docs 用語（Introduction / Gallery / Exhibit / Icon / Focus …）へコードを寄せる。

## トランジション方針（議論メモ・spec 未反映）
- 「ハッシュルーター＋DOM 差し替え」でルーティング手段としては妥当。パフォーマンスは 3 展示規模では論点にならない。
- 「View Transitions がスマホで効かない」という前提は 2026 時点では古め。**iOS Safari 18（2024/9）・Android Chrome とも対応**。非対応は古い端末の裾野のみ。VT は非対応時に「アニメ無しで即差し替え」に自動フォールバックする設計（サイトは壊れない）。
  - ※対応状況は記憶ベース。依存を決める前に caniuse で最新確認が望ましい。
- ただし演出がサイトの「キモ」なので、即差し替えフォールバックだと非対応端末で体験が丸ごと消えるのが惜しい。→ **役割で二段構え**にする方針で合意:
  - **劇場型の派手な演出（ワープ / 幕の開閉 / ハコの飛び出し）** … VT に依存しない**自前オーバーレイ**（CSS / Web Animations）。**全端末で動く**。作り込みも自由。
  - **共有要素モーフ（Gallery Icon → Focus への連続移動）** … **View Transitions を対応環境でのみ enhancement**。非対応ならモーフだけ省略、オーバーレイ演出は残るので体験は保たれる。
  - VT 一本足には**しない**。
- この方針は本人判断で **spec-and-plan.md への反映は見送り**（ログにのみ記録）。

## 次アクション
- Phase 1（命名リネーム ＋ 展示 3 件のデータ化 ＋ ハッシュルーター導入）から着手可能。

## 参照
- 仕様書/実装予定書: `ai-log/spec-and-plan.md`
